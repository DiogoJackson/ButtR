## ButtR 1.1

* Added argument `db_version` to `get_Oz_butterflies` to allow retrieval of 
  older versions of the database. Versions 1 to 3 included raw image files 
  in Sony raw form (`.ARW`), while version 4 stores raw image files in 
  Digital Negative (`.DNG`) format.

## ButtR 1.0

* First public release
